<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: pure-black window bg so there is no white flash before Compose renders -->
    <style name="Theme.RuthlessSpendGuard" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- Splash screen theme (core-splashscreen library) -->
    <style name="Theme.RuthlessSpendGuard.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#020408</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_rupee</item>
        <item name="postSplashScreenTheme">@style/Theme.RuthlessSpendGuard</item>
    </style>
</resources>
